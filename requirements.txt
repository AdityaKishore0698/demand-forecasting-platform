# Runtime + training dependencies (pinned to the versions the reported results were produced with).
numpy==2.0.2
pandas==2.3.3
PyYAML==6.0.3
lightgbm==4.6.0
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.5
